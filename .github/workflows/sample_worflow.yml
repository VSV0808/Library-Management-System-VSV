on:
  workflow_dispatch:
      inputs:
          Name:
            type: string

jobs:
  TestRun:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git config user.name ${{github.event.inputs.name}}
          git config user.email srivijay1234530@gmail.com
          git branch
          date > output.txt
          git add .
          git commit -m"sample commit using runner"
          git push
 
